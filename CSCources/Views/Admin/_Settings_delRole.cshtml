
@foreach (var item in ViewBag.roles)
{
    using (Ajax.BeginForm("_Settings_delRole", "Admin", new { id = @TempData["user_Id"], role = item }, new AjaxOptions() { UpdateTargetId = "settings" }))
    {
        <input type="submit" value=@item class="btn btn-default btn-block" />
    }
}

